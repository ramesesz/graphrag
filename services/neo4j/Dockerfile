# services/neo4j/Dockerfile
FROM neo4j:5.15.0

# Optional: If you need to copy custom config files later
# COPY neo4j.conf /var/lib/neo4j/conf/neo4j.conf

# Optional: If you want to pre-load plugins, you can COPY them here
# COPY plugins/ /plugins/

CMD ["neo4j"]